{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "node-sass": "^8.0.0",
    "nuxt": "^3.5.0",
    "sass-loader": "^13.2.2"
  },
  "dependencies": {
    "mongodb": "^5.5.0"
  }
}
